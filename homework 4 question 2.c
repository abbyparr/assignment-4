characters = input('200 characters')
character = input('character')
count = count + 1

#scan for number of occurrences of specialized character

if(input):
    count(character)

#characters do not exist in passage

if('character' != characters):
    print('invalid-enter another character')
    backup = input('backup character')
    input(backup)
       
if(characters < 200):
    print('invalid number of characters')
    passage2 = input('another passage')
    input(passage2)
   
